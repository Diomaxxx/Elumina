<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.Elumina" parent="Theme.AppCompat.Light.NoActionBar">

        <!-- Customize your light theme here -->
        <item name="colorPrimary">@color/purple_500</item>
        <item name="colorPrimaryDark">@color/purple_700</item>
        <item name="colorAccent">@color/teal_200</item>

        <!-- Colores dinámicos -->
        <item name="background_color">@color/background_color_oscuro</item>
        <item name="layout_color">@color/layout_color_oscuro</item>
        <item name="text_color">@color/text_color_oscuro</item>
        <item name="icon_color">@color/icon_color_oscuro</item>
    </style>

    <style name="Theme.Elumina" parent="Base.Theme.Elumina" />
</resources>
